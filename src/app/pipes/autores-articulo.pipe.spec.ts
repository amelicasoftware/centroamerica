import { AutoresArticuloPipe } from './autores-articulo.pipe';

describe('AutoresArticuloPipe', () => {
  it('create an instance', () => {
    const pipe = new AutoresArticuloPipe();
    expect(pipe).toBeTruthy();
  });
});
