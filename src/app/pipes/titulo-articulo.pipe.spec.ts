import { TituloArticuloPipe } from './titulo-articulo.pipe';

describe('TituloArticuloPipe', () => {
  it('create an instance', () => {
    const pipe = new TituloArticuloPipe();
    expect(pipe).toBeTruthy();
  });
});
